name: "Home Orchestrator — Battery"
version: "0.11.57"
slug: "battery_orchestrator"
description: "Carga y descarga adaptativa de baterias por precio, sol y consumo real, sin cajas negras — plugin Battery de Home Orchestrator"
url: "https://github.com/neoalarrode/Home-Orchestrator"
arch:
  - aarch64
  - amd64
  - armv7
init: false
startup: application
boot: auto
ingress: true
ingress_port: 8099
panel_icon: mdi:battery-charging-high
panel_title: "Home Orchestrator — Battery"
homeassistant_api: true
services:
  - mqtt:want
ports:
  8098/tcp: 8098
ports_description:
  8098/tcp: "Panel de solo lectura (wallpanel), sin pasar por Ingress. Desactivalo (deja el puerto vacio) si no lo vas a usar."
options: {}
schema: {}
